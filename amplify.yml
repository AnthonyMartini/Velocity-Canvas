version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - echo "GCP_LOCATION=$GCP_LOCATION" >> .env.production
        - echo "FIREBASE_SERVICE_ACCOUNT='$FIREBASE_SERVICE_ACCOUNT'" >> .env.production
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - node_modules/**/*
